accelerate
codetiming
datasets
# flash-attn>=2.4.3
liger-kernel
mathruler
numpy
omegaconf
pandas
peft
pillow
pyarrow>=15.0.0
pylatexenc
qwen-vl-utils
ray[default]

tensordict==0.8.3
# torch==2.5.1 
transformers==4.52.3  # Pin: 4.55+ fast processor breaks vLLM multimodal (0 image_pad in template text)
# transformers==4.51.0
# xformers==0.0.28.post3 # 对应 torch==2.5.1 
torch==2.6.0 
torchaudio==2.6.0  # 对应 torch==2.6.0 
torchvision==0.21.0  # 对应 torch==2.6.0 
torchdata
vllm==0.8.2 # 对应 torch==2.6.0 
# vllm>=0.7.3 
# vllm==0.7.3 # 对应 torch==2.5.1 
# vllm==0.8.4  # README requires >=0.8.3; 0.8.2 has cuBLAS FPE bug in profiling
wandb
